{
  "name": "prodisco-agent-sandbox",
  "version": "0.1.0",
  "description": "AI agent runtime packaging @prodisco/k8s-mcp for agent-sandbox deployment",
  "main": "dist/agent.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/agent.js",
    "dev": "tsc && node dist/agent.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "kubernetes",
    "mcp",
    "agent-sandbox",
    "claude",
    "ai-agent"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.53",
    "@prodisco/k8s-mcp": "^0.1.2",
    "fastify": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "@types/node": ">=20"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
